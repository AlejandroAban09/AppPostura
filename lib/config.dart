class AppConfig {
  // tu URL de Render, SIN la diagonal final
  static const String apiBase = 'https://focusme3.onrender.com';

  // tiempo máximo por petición
  static const Duration timeout = Duration(seconds: 20);
}
